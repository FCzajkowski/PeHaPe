APP_ENV=dev
APP_SECRET=your_secret_here
# When running the application on the host (not inside Docker), use the
# container's exposed host port so the app talks to the Docker DB container.
# The docker-compose maps container 3306 -> host 3307, so use 127.0.0.1:3307.
DATABASE_URL="mysql://appuser:apppass@127.0.0.1:3307/test_database"